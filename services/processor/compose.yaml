services:
  nixclip-worker:
    build: .
    container_name: nixclip-worker
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8788:8788"
    volumes:
      - ./data:/app/data
    mem_limit: 7g
    cpus: 2.0
